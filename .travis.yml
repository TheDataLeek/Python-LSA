language: python
python: 3.5
install:
    - "pip install llvmlite"
    - "pip install numpy"
    - "pip install scipy"
    - "pip install tqdm"
    - "pip install numba"
    - "pip install enforce"
    - "pip install pytest"
script: "py.test"
